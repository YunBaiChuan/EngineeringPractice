# 05月14日

## 第一题：纪年 https://www.nowcoder.com/practice/057230cf4eaa4a7eb78f70836cfb5dfb?channelPut=tracker2

**思路：模拟。以题目给定的2018年为基准，算偏移量来确定其他年份，然后多测的话不能直接II()读入，因为n可以是0，如果II()读入，则会导致提前退出**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

def solve():
    try:
        n = I()
        while n:
            n = int(n)
            diff = n - 2018
            tian = (5 + diff - 1) % 10 + 1
            di = (11 + diff - 1) % 12 + 1
            print(tian, di)
            n = I()
    except:
        pass

t = 1
# t = II()
for _ in range(t):
    solve()
```