# 11月19日

## 第一题：旅游 https://www.nowcoder.com/practice/70f173646f0a4d8daa0eac8a789e906e?channelPut=tracker2

**思路：首先注意到p具有单调性，可以用二分，这样就解决了一个子问题。然后考虑最小的连通图怎么做，应该想到最小生成树，并且我们一般写kruskal算法，用并查集，会比较简单一些。**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def GMI():
    return map(lambda x: int(x) - 1, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''
感觉是二分，p具有单调性，但是任意点两两联通这个不太清楚怎么写。
这个要用到最小生成树的kruskal算法，也就是边集来做，自然是需要用并查集了
遇到这种最小代价，两两连通就需要想到最小生成树，因为树是两两连通的
'''

class UnionFind:
    def __init__(self, n):
        self.parent_or_size = [-1] * n

    def find(self, a):
        a = self.parent_or_size[a] if self.parent_or_size[a] >= 0 else a
        acopy = a
        while self.parent_or_size[a] >= 0:
            a = self.parent_or_size[a]
        while acopy != a:
            self.parent_or_size[acopy], acopy = a, self.parent_or_size[acopy]
        return a

    def merge(self, a, b):
        pa, pb = self.find(a), self.find(b)
        if pa == pb: return False
        if self.parent_or_size[pa] > self.parent_or_size[pb]:
            pa, pb = pb, pa
        self.parent_or_size[pa] += self.parent_or_size[pb]
        self.parent_or_size[pb] = pa
        return True

    def merge_to(self, a, b):
        pa, pb = self.find(a), self.find(b)
        if pa == pb: return False
        self.parent_or_size[pb] += self.parent_or_size[pa]
        self.parent_or_size[pa] = pb
        return True

    def getSize(self, a):
        return -self.parent_or_size[self.find(a)]

    def init(self):
        for i in range(len(self.parent_or_size)):
            self.parent_or_size[i] = -1

def solve():
    n, m, c = LII()
    edges = []
    for _ in range(m):
        x, y, w = LII()
        x -= 1; y -= 1
        edges.append((w, x, y))
    edges.sort()

    l, r = 0, 10 ** 9
    while l <= r:
        mid = (l + r) >> 1
        uf = UnionFind(n)
        collect = []
        rem = n - 1 # 需要的边数
        for w, u, v in edges:
            if w > mid: # 优化，排序后如果有w > mid，那么后面的w更大
                break
            if uf.merge(u, v):
                rem -= 1
                if rem == 0:
                    break
        for w, u, v in edges:
            if rem == 0:
                break
            if w <= mid: # 前面用过了
                continue
            if uf.merge(u, v): # 不得不需要cost合并了，备选
                collect.append(w)
                rem -= 1
        if rem: # 检出能否连接成树，不行的话只能增加mid
            l = mid + 1
            continue

        collect.sort(reverse=True) # 贪心代价
        cost = 0
        for i, w in enumerate(collect, 1): # 必须用cost修的路
            cost += i * w
            if cost > c:
                break
        if cost <= c:
            r = mid - 1
        else:
            l = mid + 1
    print(l)

t = 1
for _ in range(t):
    solve()
```